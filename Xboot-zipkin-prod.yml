spring:
  application:
    name: Xboot-zipkin

  boot:
    admin:
      client:
        url: http://localhost:8084

server:
  port: 9411   #默认端口号

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  metrics:
    web:
      server:
        auto-time-requests: false